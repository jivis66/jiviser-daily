# AGENTS.md

## 项目概述

本项目是一个基于能力图谱实现的**完美个性化日报信息收集 Agent**，为用户提供智能、高效、个性化的日报信息收集和推送服务。

**项目名称**: Daily Agent / openclaw-skills-daily  
**项目语言**: Python  
**项目语言**: 中文  
**许可证**: MIT License  
**仓库性质**: 代码实现仓库 + 能力规范文档

## 文档结构

```
.
├── README.md                   # 项目简介与使用指南
├── perfect-daily-agent.md      # 核心文档：能力图谱详细定义
├── AGENTS.md                   # 本文件：项目开发指南
├── LICENSE                     # MIT 许可证
├── requirements.txt            # Python 依赖
├── docker-compose.yml          # Docker 部署配置
├── Dockerfile                  # Docker 镜像构建
├── config/                     # 配置文件目录
│   ├── columns.yaml            # 分栏配置
│   ├── sources_example.yaml    # 数据源配置示例
│   └── templates.yaml          # 模板配置
├── src/                        # 源代码目录
│   ├── collector/              # 采集模块
│   ├── processor/              # 处理模块
│   ├── filter/                 # 筛选排序模块
│   ├── output/                 # 输出模块
│   ├── personalization/        # 个性化模块
│   ├── auth_manager.py         # 认证管理
│   ├── cli.py                  # 命令行工具
│   ├── config.py               # 配置管理
│   ├── database.py             # 数据库模型
│   ├── llm_config.py           # LLM 配置
│   ├── main.py                 # FastAPI 入口
│   ├── models.py               # 数据模型
│   ├── scheduler.py            # 任务调度
│   ├── service.py              # 业务服务
│   └── setup_wizard.py         # 设置向导
├── tests/                      # 测试目录
└── .env.example                # 环境变量示例
```

## 核心能力实现

本项目实现了 [perfect-daily-agent.md](perfect-daily-agent.md) 定义的 8 大能力维度：

### 1. 信息收集能力 ✅
- **RSS 采集**: 支持标准 RSS/Atom 订阅源
- **API 采集**: Hacker News、GitHub、arXiv、Hugging Face 等
- **社交媒体**: B站、小红书、即刻、知乎等
- **新闻源**: 财新、FT中文网、第一财经、界面等
- **音频播客**: 支持播客内容采集

### 2. 个性化能力 ✅
- **用户画像**: 兴趣偏好、阅读历史、互动行为
- **冷启动**: 预设模板支持新用户
- **自适应学习**: 基于反馈调整推荐

### 3. 内容理解与处理 ✅
- **内容清洗**: HTML 清洗、格式化
- **关键词提取**: 基于 NLP 的关键词识别
- **主题分类**: 自动内容分类
- **自动摘要**: LLM 驱动的智能摘要

### 4. 信息筛选与排序 ✅
- **智能去重**: 语义去重、精确去重
- **质量评分**: 基于多维度质量评估
- **个性化排序**: 结合用户画像的排序算法
- **多样性保证**: 避免内容单一化

### 5. 输出与呈现 ✅
- **多格式输出**: Markdown、HTML、JSON
- **推送渠道**: Telegram、Slack、Discord、邮件
- **模板系统**: 可自定义输出模板

### 6. 交互与反馈 ✅
- **反馈收集**: 点赞/点踩、评分
- **REST API**: 完整的 API 接口
- **CLI 工具**: 命令行管理工具

### 7. 系统与工程能力 ✅
- **异步架构**: 基于 asyncio 的高性能采集
- **数据存储**: SQLite/PostgreSQL + SQLAlchemy
- **定时任务**: APScheduler 任务调度
- **Docker 部署**: 容器化支持
- **配置热更新**: 无需重启的配置更新

### 8. 智能化能力 ✅
- **LLM 集成**: OpenAI、Claude、Ollama、Azure、国产模型
- **智能摘要**: 多档摘要生成（1句/3点/段落）
- **智能推荐**: 基于内容的个性化推荐

## 技术栈

| 层级 | 技术 |
|------|------|
| **Web 框架** | FastAPI、Uvicorn |
| **数据库** | SQLite（默认）/ PostgreSQL、SQLAlchemy 2.0 |
| **数据验证** | Pydantic v2 |
| **任务调度** | APScheduler |
| **HTTP 客户端** | HTTPX、aiohttp |
| **HTML 解析** | BeautifulSoup4 |
| **RSS 解析** | feedparser |
| **NLP/ML** | scikit-learn、numpy |
| **CLI** | Click、Rich |
| **部署** | Docker、Docker Compose |

## 项目架构

```
┌─────────────────────────────────────────────────────────────┐
│                        FastAPI API                          │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ /reports│ │/collect │ │/profile │ │/feedback│           │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘           │
├───────┴───────────┴───────────┴───────────┴─────────────────┤
│                    DailyAgentService                         │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ Collector│  │Processor │  │  Filter  │  │  Output  │    │
│  │  采集模块 │  │ 处理模块 │  │ 筛选排序 │  │ 输出模块 │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────┐    │
│  │Personalization│  │  Scheduler   │  │     LLM        │    │
│  │   个性化模块   │  │   任务调度   │  │   LLM 配置     │    │
│  └──────────────┘  └──────────────┘  └────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│                    Database (SQLite/PG)                      │
└─────────────────────────────────────────────────────────────┘
```

## 开发指南

### 环境设置

```bash
# 克隆仓库
git clone https://github.com/uhajivis-cell/openclaw-skills-daily.git
cd openclaw-skills-daily

# 创建虚拟环境
python -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件
```

### 代码规范

- **类型注解**: 使用 Python 类型注解
- **异步编程**: IO 操作使用 asyncio
- **依赖注入**: FastAPI 依赖注入模式
- **异常处理**: 统一的异常处理机制

### 添加新的采集器

1. 在 `src/collector/` 创建新的采集器类，继承 `BaseCollector`
2. 实现 `collect()` 方法
3. 在配置文件中添加数据源配置

示例：
```python
from src.collector.base import BaseCollector, CollectResult

class MyCollector(BaseCollector):
    async def collect(self) -> CollectResult:
        # 实现采集逻辑
        pass
```

### 添加新的推送渠道

1. 在 `src/output/publisher.py` 中添加新的 Publisher 实现
2. 实现 `publish()` 方法
3. 在 `ChannelType` 中添加新渠道类型

### 测试

```bash
# 运行测试
pytest

# 运行特定测试
pytest tests/test_collector.py
```

## 配置说明

### 分栏配置 (`config/columns.yaml`)

定义日报的各个分栏及其数据源：

```yaml
columns:
  - id: "headlines"
    name: "🔥 今日头条"
    enabled: true
    max_items: 5
    sources:
      - type: "rss"
        name: "TechCrunch"
        url: "https://techcrunch.com/feed/"
```

### 环境变量

关键配置项：

| 变量 | 说明 | 必需 |
|------|------|------|
| `OPENAI_API_KEY` | LLM API 密钥 | 可选（推荐） |
| `DATABASE_URL` | 数据库连接 | 可选 |
| `DEFAULT_PUSH_TIME` | 默认推送时间 | 可选 |
| `TELEGRAM_BOT_TOKEN` | Telegram 推送 | 可选 |
| `SLACK_BOT_TOKEN` | Slack 推送 | 可选 |

## 部署

### Docker 部署（推荐）

```bash
docker-compose up -d
```

### 本地运行

```bash
uvicorn src.main:app --reload
```

### CLI 使用

```bash
# 生成日报
python -m src.cli generate

# 触发采集
python -m src.cli collect

# 推送日报
python -m src.cli push <report_id>
```

## 演进状态

根据 [perfect-daily-agent.md](perfect-daily-agent.md) 的演进路线：

- [x] **Phase 1**: 基础采集 → 简单过滤 → 定时推送
- [x] **Phase 2**: 多源聚合 → 自动摘要 → 基础个性化
- [x] **Phase 3**: 智能推荐 → 用户画像 → 多格式输出
- [x] **Phase 4**: LLM 增强 → 智能问答 → 持续学习（部分实现）
- [ ] **Phase 5**: 多模态 → 预测性推荐 → 生态集成

## 相关链接

- 项目主页: https://github.com/uhajivis-cell/openclaw-skills-daily
- 能力图谱: `perfect-daily-agent.md`
- API 文档: 启动后访问 `/docs`

## 贡献指南

1. **Bug 修复**: 直接提交 PR
2. **功能增强**: 先创建 Issue 讨论
3. **新采集器**: 参考现有实现，确保包含测试
4. **文档更新**: 同步更新 README.md 和 AGENTS.md

---

*本文档面向 AI 编程助手，用于快速理解本项目的目的、结构和开发规范。*
